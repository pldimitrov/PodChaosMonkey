apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-workload.fullname" . }}-test-connection"
  labels:
    {{- include "test-workload.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-workload.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
